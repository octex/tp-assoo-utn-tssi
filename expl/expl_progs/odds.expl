int main()
{
decl
    int temp,num,resto;
enddecl
begin
    num=1;
    while ( num <= 100 ) do
        resto = num % 2;
        if ( resto != 0 ) then
            temp = exposcall ( "Write" , -2, num );
        endif;
         num = num + 1;
    endwhile;
    return 0;
end
}
